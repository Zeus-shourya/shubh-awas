# Local editor / tooling config
.claude/

# Internal notes. A public GitHub repo renders README.md on the repo homepage,
# and this one carries the pre-launch checklist, including the note that the
# headline figures are still placeholders. Keep it on your machine.
# If you'd rather publish it, delete this line and commit it.
README.md

# Build/upload artefacts
*.zip
